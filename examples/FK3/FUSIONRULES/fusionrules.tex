\subsubsection{$ M(e,-)\otimes -$}
\begin{align*}
M(e,-) &\otimes M(e,-) \simeq M(e,+) &&& M(e,-) &\otimes M(e,\rho) \simeq M(e,\rho)\\
M(e,-) &\otimes M(\sigma,+) \simeq M(\sigma,-) &&& M(e,-) &\otimes M(\sigma,-) \simeq M(\sigma,+)\\
M(e,-) &\otimes M(\tau,0) \simeq M(\tau,0) &&& M(e,-) &\otimes M(\tau,1) \simeq M(\tau,1)\\
M(e,-) &\otimes M(\tau,2) \simeq M(\tau,2)
\end{align*}

\subsubsection{$ M(e,\rho)\otimes -$}
\begin{align*}
M(e,\rho) &\otimes M(e,\rho) \simeq M(e,+) \oplus M(e,-) \oplus M(e,\rho) &&& M(e,\rho) &\otimes M(\sigma,+) \simeq M(\sigma,+) \oplus M(\sigma,-)\\
M(e,\rho) &\otimes M(\sigma,-) \simeq M(\sigma,+) \oplus M(\sigma,-) &&& M(e,\rho) &\otimes M(\tau,0) \simeq M(\tau,1) \oplus M(\tau,2)\\
M(e,\rho) &\otimes M(\tau,1) \simeq M(\tau,0) \oplus M(\tau,2) &&& M(e,\rho) &\otimes M(\tau,2) \simeq M(\tau,0) \oplus M(\tau,1)\\
\end{align*}

\subsubsection{$ M(\sigma,+)\otimes -$}
\begin{align*}
M(\sigma,+) &\otimes M(\sigma,+) \simeq M(e,+) \oplus M(e,\rho) \oplus M(\tau,0) \oplus M(\tau,1) \oplus M(\tau,2) &&& M(\sigma,+) &\otimes M(\sigma,-) \simeq M(e,-) \oplus M(e,\rho) \oplus M(\tau,0) \oplus M(\tau,1) \oplus M(\tau,2)\\
M(\sigma,+) &\otimes M(\tau,0) \simeq M(\sigma,+) \oplus M(\sigma,-) &&& M(\sigma,+) &\otimes M(\tau,1) \simeq M(\sigma,+) \oplus M(\sigma,-)\\
M(\sigma,+) &\otimes M(\tau,2) \simeq M(\sigma,+) \oplus M(\sigma,-)
\end{align*}

\subsubsection{$ M(\sigma,-)\otimes -$}
\begin{align*}
M(\sigma,-) &\otimes M(\sigma,-) \simeq M(e,+) \oplus M(e,\rho) \oplus M(\tau,0) \oplus M(\tau,1) \oplus M(\tau,2) &&& M(\sigma,-) &\otimes M(\tau,0) \simeq M(\sigma,+) \oplus M(\sigma,-)\\
M(\sigma,-) &\otimes M(\tau,1) \simeq M(\sigma,+) \oplus M(\sigma,-) &&& M(\sigma,-) &\otimes M(\tau,2) \simeq M(\sigma,+) \oplus M(\sigma,-)\\
\end{align*}

\subsubsection{$ M(\tau,0)\otimes -$}
\begin{align*}
M(\tau,0) &\otimes M(\tau,0) \simeq M(e,+) \oplus M(e,-) \oplus M(\tau,0) &&& M(\tau,0) &\otimes M(\tau,1) \simeq M(e,\rho) \oplus M(\tau,2)\\
M(\tau,0) &\otimes M(\tau,2) \simeq M(e,\rho) \oplus M(\tau,1)
\end{align*}

\subsubsection{$ M(\tau,1)\otimes -$}
\begin{align*}
M(\tau,1) &\otimes M(\tau,1) \simeq M(e,+) \oplus M(e,-) \oplus M(\tau,1) &&& M(\tau,1) &\otimes M(\tau,2) \simeq M(e,\rho) \oplus M(\tau,0)\\
\end{align*}

\subsubsection{$ M(\tau,2)\otimes -$}
\begin{align*}
M(\tau,2) &\otimes M(\tau,2) \simeq M(e,+) \oplus M(e,-) \oplus M(\tau,2)
\end{align*}

